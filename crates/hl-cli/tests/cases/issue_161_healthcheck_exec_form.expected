--- compose ---
services:
  miniflux-db:
    image: postgres:15
    command:
    - postgres
    - -c
    - max_connections=50
    healthcheck:
      test:
      - CMD
      - pg_isready
      - -U
      - miniflux
      interval: 10s
      retries: 3
      start_period: 30s
