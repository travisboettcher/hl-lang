--- warnings ---
issue_232_raw_labels_replace_router.hll:20:5: warning: `raw { labels: ... }` replaces service `web`'s computed labels rather than adding to them, so every entry its `labels` blocks and the templates it applies would have produced is dropped — write the extra labels in a `labels { ... }` block instead, or reproduce the ones you still need in this list
--- compose ---
services:
  web:
    image: test
    expose:
    - 8123
    labels:
    - traefik.http.routers.web-internal.tls.domains[0].main=internal.example.com
